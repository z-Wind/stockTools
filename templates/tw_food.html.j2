<!DOCTYPE html>
<html data-bs-theme="dark">
    <head>
        <title>{{title}}</title>
        <meta charset="UTF-8">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
    </head>
    <body bgcolor="#000000">
        資料來源：<a href="https://consumer.fda.gov.tw/Food/TFND.aspx?nodeID=178" target="_blank">衛生福利部食品藥物管理署</a><br/>
        <a href="https://data.gov.tw/dataset/8543" target="_blank">政府資料開放平臺：食品營養成分資料集</a><br/>
        {% if kinds %}
        <h2>{{kinds.0}} {{kinds.1}} 每100g含量，單位 {{kinds.2}}</h2>
        {% else %}
        <h2>每100g含量</h2>
        {% endif %}
        {% for name,link in links.items() %}
        <div class="d-grid gap-2">
            <a class="btn btn-outline-light" href="{{ link }}" target="_blank">{{ name }}</a>
        </div>
        {%- endfor %}            
             
        {% for name,table in tables.items() %}
        <div class="d-grid gap-2">
            <button class="btn btn-outline-light" type="button" data-bs-toggle="collapse"  data-bs-target="#collapse{{ loop.index }}" aria-expanded="false" aria-controls="collapse{{ loop.index }}">
                {{ name }}
            </button>
        </div>
        <div id="collapse{{ loop.index }}" class="collapse">
            {{ table }}
        </div>
        </div>
        {%- endfor %}

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q" crossorigin="anonymous"></script>
    </body>
</html>
